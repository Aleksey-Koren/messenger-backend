package com.example.whisper.service.producer;

import org.springframework.cloud.stream.annotation.Output;
import org.springframework.messaging.MessageChannel;

public interface MessageProducerChannels {
    // String BROADCAST = "broadcastMessagings";

    // @Output(BROADCAST)
    // MessageChannel broadcastMessagings();
}
